#!/bin/sh
set -eu

case "$1" in
  remove|deconfigure)
    systemctl stop spi-bridge.service >/dev/null 2>&1 || true
    systemctl disable spi-bridge.service >/dev/null 2>&1 || true
    ;;
esac

exit 0
